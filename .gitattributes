# Normalize line endings to LF on commit. Emitters produce LF; golden
# snapshots are byte-for-byte contracts, so a stray CRLF breaks the tests.
* text=auto eol=lf

# Binary assets must never be normalized.
*.png binary
*.svg -text
